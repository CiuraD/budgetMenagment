spring:
  datasource:
    url: jdbc:postgresql://budget-db:5432/budgetdb
    username: budget
    password: budgetpass

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  redis:
    host: redis
    port: 6379

server:
  port: 8082

jwt:
  secret: change-this-secret-to-a-strong-random-value
  expiration-ms: 86400000 # 1 day
